PROGRAM Example3(cats, dogs);

USES math, manyCats IN './manyCats.pas';

VAR
age : INTEGER;
cat : CHAR = 'c';
dog : BOOLEAN = 1;
length : REAL;

FUNCTION f;
BEGIN
   IF a = 6 THEN b := 7
END

FUNCTION h;
BEGIN
   f(9);
   IF j < 10 THEN f(9) ELSE f(0)
END

BEGIN
    f(8, 9);
    Writeln('cats');

    FOR i := 1 TO 10
    DO Writeln(i);

    FOR i := 10 DOWNTO 1
    DO REPEAT
            h('cats');
            a += 7
       UNTIL g <= 1
END.
